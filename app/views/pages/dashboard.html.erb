<!-- app/views/pages/dashboard.html.erb -->
<div class="max-w-xl mx-auto py-10 px-4">
  <h1 class="text-3xl font-semibold mb-3">Dashboard</h1>
  <p class="text-gray-600 mb-6">
    Bun venit,
    <strong><%= Current.user&.email_address || current_user&.email_address %></strong>!
  </p>

  <div class="flex gap-3">
    <%= link_to "Home", root_path,
        class: "px-4 py-2 rounded-md border border-gray-300" %>

    <%= button_to "Logout", session_path, method: :delete,
        class: "px-4 py-2 rounded-md bg-gray-900 text-white" %>
  </div>
</div>
